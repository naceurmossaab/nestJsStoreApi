export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}

export enum OrderStatus {
  PENDING = 'pending',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered'
}

export enum Services {
  AUTH = "auth",
  USERS = "users",
  PRODUCTS = "products",
  CART = "cart",
  WISHLIST = "wishlist",
  ORDER = "order",
}